#ifndef UTILS_H
#define UTILS_H


/**
 * @brief Function to setup catalog
 * @param folder the folder where the csv is located
 * return 0 if success, -1 if fail
*/
int setup_catalogs(char* folder);

#endif