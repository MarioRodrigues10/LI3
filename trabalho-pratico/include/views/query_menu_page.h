#ifndef QUERY_MENU_PAGE_H
#define QUERY_MENU_PAGE_H

/**
 * @brief Functions that implement the query menu.
 *
 * @param line The line to be filled with the query number and its arguments.
 *
 * @return The chosen option.
 */
int query_menu_page(char *line);

#endif
