#ifndef TITLE_ONLY_PAGE_H
#define TITLE_ONLY_PAGE_H

/**
 * @brief Function to display a page with only a title.
 *
 * @param title The title of the page.
 * @param time The time to wait before returning to the next page.
 */
void title_only_page(char *title, int time);

#endif
