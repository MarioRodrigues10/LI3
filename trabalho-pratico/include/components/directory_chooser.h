#ifndef DIRECTORY_CHOOSER_H
#define DIRECTORY_CHOOSER_H

/**
 * @brief Function that allows the user to choose a directory.
 *
 * @param dataset_path The path of the directory chosen by the user.
 */
void choose_directory(char* dataset_path);

#endif
